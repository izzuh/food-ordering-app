import { api } from './api';
export interface PaymentInitialization { payment: { id: string; order_id: string; reference: string; amount_minor: number; currency: string; status: string }; checkout: { id: string; client_secret: string; status: string }; }
export async function initializePayment(accessToken: string, orderId: string): Promise<PaymentInitialization> { const response = await api.post<{ success: true; data: PaymentInitialization }>('/payments/initialize', { orderId }, { headers: { Authorization: `Bearer ${accessToken}` } }); return response.data.data; }
export async function verifyPayment(accessToken: string, paymentIntentId: string) { const response = await api.get(`/payments/verify/${encodeURIComponent(paymentIntentId)}`, { headers: { Authorization: `Bearer ${accessToken}` } }); return response.data.data; }
